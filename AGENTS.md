# دليل العمل في مشروع LOAD CV

## الهدف والنطاق

هذا المستودع هو منصة `LOAD CV` العربية/الإنجليزية لبناء السير الذاتية، تحريرها، تصديرها، إرسالها بالبريد، واستخدام أدوات ذكاء اصطناعي مدفوعة بالنقاط. يتضمن أيضًا اشتراكات Paddle، التحويل البنكي، حساب المستخدم، الدعم، ولوحة إدارة.

قبل أي تعديل وظيفي، راجع `docs/PROJECT_STATE.md` و`docs/DECISIONS.md` مع الكود المرتبط مباشرة بالمهمة. لا تعتبر `README.md` أو `PROJECT_MAP.md` مصدرًا حديثًا للحالة؛ فهما يحتويان أجزاءً متبقية من قالب البداية.

## التقنية والإصدارات

- Next.js `15.5.18` مع App Router وReact `19.1.5` وTypeScript `5.9.2`.
- Node.js: `>=20` حسب `package.json`، و`.nvmrc` يطلب Node 20. استخدم `pnpm`.
- واجهة: Tailwind CSS `4.1.13`، Radix UI، مكونات shadcn/ui محلية، و`lucide-react` للأيقونات.
- الهوية والجلسات والبيانات: Supabase عبر `@supabase/ssr` و`@supabase/supabase-js`.
- الفوترة: Paddle Billing عبر `@paddle/paddle-js` و`@paddle/paddle-node-sdk`.
- الذكاء الاصطناعي: OpenRouter عبر Vercel AI SDK (`ai` و`@ai-sdk/*`).
- البريد: Resend API. التصدير المحلي لـPDF يعتمد `html2pdf.js`، واستيراد PDF النصي يعتمد `pdf-parse`.

## هيكلة المجلدات

- `src/app`: الصفحات وRoute Handlers؛ كل منطق HTTP يُحفظ هنا.
- `src/components`: واجهات المنتج، ومنها `resume-builder` و`admin` و`dashboard` و`billing`.
- `src/lib`: قواعد العمل المشتركة مثل الاستحقاق الفعلي، الذكاء الاصطناعي، الإدارة، الإشعارات والبريد.
- `src/utils/supabase`: عملاء Supabase للمتصفح والخادم والخادم الداخلي.
- `src/utils/paddle`: عميل Paddle ومساعدات الويب هوك وقراءة الفوترة.
- `src/constants` و`src/hooks` و`src/styles`: الثوابت والخطافات والأنماط العامة.
- `supabase/migrations`: مصدر الحقيقة المحلي لتغييرات مخطط قاعدة البيانات.
- `supabase/functions/process-account-deletions`: Edge Function لحذف الحسابات المستحقة.
- `public/brand` و`public/assets`: الشعار والملفات البصرية.
- `docs`: حالة المشروع وقراراته. حدّثها عند تغيير سلوك أو قرار مؤثر.

## معايير كتابة الكود

- استخدم TypeScript صريحًا للبيانات العابرة للحدود: Route Handlers، استجابات AI، Paddle، وSupabase.
- فضّل المكونات والخدمات الموجودة على إنشاء طبقة بديلة. لا تنقل منطق الأعمال إلى مكونات العميل إذا كان يمكن أن يكون خادميًا.
- النصوص العربية والإنجليزية يجب أن تكون UTF-8 سليمة، وتُراجع في RTL وLTR. لا تضف نصوصًا عربية مشوهة الترميز.
- استخدم `lucide-react` للأيقونات. احترم ألوان وهوية LOAD: `LOAD` و`LOAD CV` وعبارة "Load سيرتك، وابدأ مسيرتك".
- استخدم `@/` للاستيرادات الداخلية. حافظ على تنسيق Prettier الحالي ولا تنفذ تنسيقات واسعة غير مرتبطة بالمهمة.
- اجعل الأخطاء الخادمية آمنة للمستخدم، وسجل التفاصيل التقنية في الخادم فقط.
- عند التعامل مع Supabase، طبّق RLS على الجداول المعروضة، ولا تستخدم `user_metadata` للتفويض. تحقق من المستخدم خادميًا قبل أي عملية حساسة.
- عند التعامل مع Paddle، تحقّق من ملكية العميل/الاشتراك على الخادم، واعتبر الـwebhook الموثّق مصدر الحالة النهائي.

## أوامر التطوير والتحقق

```powershell
pnpm install --frozen-lockfile
pnpm dev -- --port 3000
pnpm exec tsc --noEmit
pnpm lint
pnpm prettier:check
pnpm test
pnpm build
```

`pnpm test` يشغل ESLint ثم فحص Prettier. لا تشغّل تصحيح ESLint أو Prettier على كامل المستودع إلا عندما يكون ذلك مطلوبًا ومراجعًا.

## البيئة والأسرار

- استخدم `.env.example` مرجعًا للأسماء، واحتفظ بالقيم الفعلية في `.env.local` فقط.
- لا تطبع أو تلتزم بمفاتيح Supabase service role أو Paddle API/webhook secret أو OpenRouter أو Resend.
- يجب أن يبدأ Paddle Sandbox صراحةً بـ`NEXT_PUBLIC_PADDLE_ENV=sandbox`، ويبدأ client token الخاص به بـ`test_`.
- `ADMIN_EMAILS` قائمة خادمية مفصولة بفواصل؛ لا تستبدلها بصلاحية داخل `user_metadata`.

## قرارات ومحظورات لا تغيّر دون إذن صريح

- لا تحذف أو تقترح تنظيف منتجات/أسعار Paddle، وجهة إشعار Paddle، العملاء، الاشتراكات، المعاملات، أو سجلات الفوترة المعكوسة في Supabase. هذه بنية تشغيلية دائمة.
- لا تغيّر بيئة Paddle تلقائيًا، ولا تجعل متغيرات البيئة الحساسة متاحة عبر `NEXT_PUBLIC_*`.
- لا تتجاوز توقيع Paddle webhook ولا تحلل جسم الطلب JSON قبل التحقق؛ يجب تمرير الجسم الخام إلى Paddle SDK.
- لا تعامل إلغاءً مجدولًا على أنه إلغاء فعلي؛ الوصول المدفوع يبقى متاحًا لـ`active` و`trialing` إلى أن تصبح الحالة نهائية.
- لا تغيّر استحقاق المستخدم مباشرةً كبديل عن webhook عند إلغاء Paddle؛ اطلب العملية من Paddle واترك webhook يزامن الحالة.
- لا تحذف حسابًا فورًا: المسار المعتمد هو طلب، مهلة سبعة أيام، إمكان التراجع، ثم Edge Function.
- لا تعدّل ترحيلات سابقة مطبقة. أضف ترحيلًا جديدًا، وراجع RLS والصلاحيات والفهارس.
- لا تغيّر أسعار أو حدود الباقات أو منطق النقاط دون تحديث `docs/DECISIONS.md` و`docs/PROJECT_STATE.md` واختبارات التدفق.

## قبل التسليم

- شغّل فحص الأنواع وESLint وPrettier على الأقل، وبناء الإنتاج عند تغييرات واسعة.
- افحص الشاشات المتأثرة بالعربية والإنجليزية، وعلى سطح المكتب والجوال عند تغيير الواجهة.
- اختبر التفويض من حساب غير إداري عند تعديل الإدارة أو البيانات الخاصة.
- حدّث وثائق الحالة والقرارات عندما يتغير سلوك ظاهر أو عقد بيانات أو قرار معماري.
